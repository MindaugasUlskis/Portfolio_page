import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Check the path to your App component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
